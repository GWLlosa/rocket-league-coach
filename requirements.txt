# Minimal requirements for basic functionality
# We'll add carball back once the system is working

# Core API functionality
python-ballchasing>=0.4.1

# Web framework and server
fastapi>=0.68.0
uvicorn[standard]>=0.15.0
jinja2>=3.0.0

# Data processing - use pre-built wheels only
pandas>=1.3.0
numpy>=1.21.0

# HTTP client for API calls
httpx>=0.24.0
aiohttp>=3.8.0

# Configuration and environment
python-decouple>=3.4
pydantic>=1.8.0

# CLI interface
click>=8.0.0
rich>=12.0.0

# Essential utilities
python-dotenv>=0.19.0
tenacity>=8.0.0

# Testing
pytest>=6.2.0

# Logging
structlog>=21.1.0

# NOTE: carball temporarily removed due to dependency conflicts
# We'll add it back once basic system is working
# carball>=0.7.5
